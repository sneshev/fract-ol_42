#include "fractol.h"

t_data *init_julia(char *ala, char *bala)
{
    // t_data *data;

    // data = (t_data *)malloc(sizeof (t_data));
    // if (!data)
    //     return (NULL);


    return (printf("AREBATEEE"), NULL);
}